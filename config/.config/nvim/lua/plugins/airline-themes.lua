return {
  "vim-airline/vim-airline-themes",
  dependencies = {
    "vim-airline/vim-airline", -- Ensure vim-airline is installed
  },
  lazy = false, -- Load immediately to ensure themes are available
}
